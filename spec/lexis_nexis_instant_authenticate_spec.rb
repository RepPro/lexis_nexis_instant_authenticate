require 'spec_helper'

describe LexisNexisInstantAuthenticate do
  it 'has a version number' do
    expect(LexisNexisInstantAuthenticate::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
